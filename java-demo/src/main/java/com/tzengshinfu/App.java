package com.tzengshinfu;

import java.math.BigDecimal;

public class App {
    static String memberStr = "memberStr";

    public static void main( String[] args ) {
        memberStr       // <--- Click the space behind the variable `memberStr` to generate completion items.
        var localStr = "localStr";
        localStr       // <--- Click the space behind the variable `localStr` to generate completion items.
        var localStrArray = new String[]{};
        localStrArray       // <--- Click the space behind the variable `localStrArray` to generate completion items.
        var localStrArrayLength = localStrArray.length;
        localStrArrayLength       // <--- Click the space behind the variable `localStrArrayLength` to generate completion items.
        var bigdecimal1 = new BigDecimal("1234");
        var bigdecimal2 = new BigDecimal("2");
        var bigdecimal3 = BigDecimal.valueOf(0);
        bigdecimal1       bigdecimal2       bigdecimal3       // <--- Click the space behind the variable `bigdecimal1/bigdecimal2/bigdecimal3` to generate completion items.
        var localLongStr = """
            localLongStr
        """;
        localLongStr       // <--- Click the space behind the variable `localLongStr` to generate completion items.
    }
}
